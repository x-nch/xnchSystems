# Documentation Sync — 2026-08-27/28 Feature Backlog Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Bring `docs/`, `README.md`, and `AGENTS.md` current with the 2026-08-27/28 changes (OpenCode Go inference migration, xtrain Phase 1 training cycle, nexi dynamic persona + refresh timer, xnch_mcp chat-loop fixes, workflow canvas + server-side HITL gate, top-level repo structure).

**Architecture:** Six independent doc tasks, each a self-contained edit + verify + commit cycle. All content is sourced from verified commits and submodule code inspected on 2026-09-02 (facts bank below). No doc restructure — the existing `docs/architecture|guides|reference|runbooks` layout is extended in place.

**Tech Stack:** Markdown docs only. Verification via `rg` content assertions and relative-link checks (no pytest).

**Spec:** Audit findings recorded 2026-09-02 in this session — docs were overhauled 2026-08-23/24 (`005568f`, `bebb01a`, `091b39f`, `255d5d6`, `b6635e5`, `7453844`); only `docs/architecture/training.md` (3416a4f), `docs/architecture/elizaos-fit.md` (73e2bbd), `docs/runbooks/node-b-hardware-gate.md` (c905c30), and the agentic-10-10 spec/plan were touched after 08-26. Everything from 08-27/28 is undocumented.

## Global Constraints

- Every factual claim must trace to a commit hash in the Facts bank, or to a file the executor re-reads before writing. Do not invent env var names, defaults, or endpoints.
- Env var tables match the anchor style already in `docs/reference/env-vars.md` (`## XNCH_`, `## NEXI_`, `## XTRAIN_`).
- Relative links only inside `docs/` (`../reference/env-vars.md#xnch_` style).
- New pages allowed only: `docs/guides/nexi-persona.md` and `docs/runbooks/persona-refresh.md` (Task 4). Everything else extends existing files.
- Match existing doc voice: tables for reference data, short imperative runbooks with `Run:`/`Expected:` pairs.
- One commit per task, conventional style (`docs: ...`). Never commit submodule pointers (xnch/nexi) as part of these tasks.

## Facts Bank (verified 2026-09-02 — do not re-derive)

- **OpenCode Go (xnch):** `xnch/config.py:96-107` — `opencode_go_api_url` default `https://opencode.ai/zen/go/v1`, `opencode_go_api_key` (empty default), `opencode_go_api_timeout_s` `60.0`, `litellm_proxy_url` (kept), `llm_status_url` default `https://opencode.ai/zen/go/v1/models`, `llm_model_id` default `deepseek-v4-pro`, `llm_probe_timeout_s` `5.0`. Commits: `fcdedf1` (migrate inference calls), `3d54e21` (local-first routing restored), `51a315c` (chat uses configured `llm_model_id`, not routing label).
- **OpenCode Go (nexi):** `nexi/config.py:18-43` — `NEXI_OPENCODE_GO_API_URL/_API_KEY/_API_TIMEOUT_S` trio, a dynamic-model-selection budget setting, `opencode_go_models` catalog override (list of dicts), retained `vllm_primary_url`/`vllm_secondary_url`/`litellm_proxy_url` fallbacks. Commits: `92bd45a` (option/model generation via OpenCode Go), `a40d63e` (configured hosted model), `c375d47` (stable-prefix prompt segmentation + caching).
- **xnch_mcp (08-28):** `75c7380` concrete file paths routed straight to `fs_read`; `5a33c09` noise dirs pruned from recursive fs glob; `d2ca850` tool results surfaced in chat loop + Office document text extraction; `3b18021` never send unsupported `tool_choice` to OpenCode Go.
- **xtrain Phase 1 (08-27):** `3416a4f` Node B isolated QLoRA venv (`~/venvs/xtrain`, `infra/no-k3s/node-b/scripts/setup-xtrain-venv.sh`); `1ce8a38` `train/qlora.py` (QLoRA SFT + G1 resolution); `a218c61` `train/merge.py` (adapter merge + GPTQ requant) + `train/registry.py` (immutable checkpoint registry); `3ea7514` GPU-exclusive cycle/promote units + 3-way conflict wiring; `362e9e5` goal-claim + HITL promotion proposal client; `07b7c2a` raise_for_status on `/goals/claim` POST; `58a982e` `train/promote.py` (symlink flip + smoke + rollback) + cycle lockfile; `b132795` cycle orchestrator (`run_cycle` at `xnch-train/xnch_train/train/cycle.py:39`, `_acquire_lock` at :113; CLI verbs `cycle` and `promote` in `xnch-train/xnch_train/cli.py`); `11418c3` cycle observability traces + GPU contention + disk-quota alarm. Phase 0 doc `docs/architecture/training.md` CLI section lists only `validate-dataset`, `extract`, `suite`, `baseline`.
- **xtrain config defaults:** `xnch-train/xnch_train/config.py:33-41` — `dataset_dir` `./datasets`, `postgres_url` `postgresql://localhost:5432/xnch`, `langfuse_host/_public_key/_secret_key` empty, `pseudonymize_secret` empty (required, fail-fast), `gate_epsilon` `0.02`, `serving_regression_bound_pct` `10.0`, `extract_page_size` `100`.
- **nexi persona (08-27/28):** `f84a8e9` 100% dynamic & realtime persona + per-task model router; `eefe005` standalone refresh CLI `nexi/character/refresh.py` (argparse, `--verbose`, "Refresh nexi persona + capabilities overlays"); `nexi/config.py:75-76` `persona_auto_refresh` default `True`, `persona_generated_path` default `~/.xnch/nexi-persona.generated.yaml`; inputs `nexi/character/persona.yaml`, `capabilities.yaml`, `identity_facts.yaml`; infra timer `infra/no-k3s/node-a/systemd/nexi-persona-refresh.timer` (`f6d7d9d`) with xnch-venv fix (`25e937c`, resolves `prometheus_client`). Spec: `docs/superpowers/specs/2026-08-27-dynamic-nexi-persona-design.md`.
- **Workflows (08-24):** `a059090` drag-and-drop workflow canvas builder (web); `fb82192` server-side HITL gating enforced in xnch. Existing `docs/guides/build-workflow.md`, `docs/architecture/workflows-hitl.md`, `docs/guides/operate-hitl.md` date to 08-23 with zero matches for "canvas"/"drag".
- **Top-level structure:** actual dirs: `agent-runner/ cli/ deepseek-harness/ docs_test_mcp/ exec_agent/ fs_read_agent/ infra/ mcp/ misc/ nexi/ research_mcp_ecosystem_2026-08/ scraper/ scripts/ skills/ tests/ web/ xnch_mcp/ xnch-train/ xnch/`. `README.md` (overhauled 08-23) lists xnch-train/xnch_mcp/scraper/web but not `agent-runner/`. `AGENTS.md` "Multi-Repo Structure" still lists only `xnch/ nexi/ infra/ docs/ scripts/ misc/` and says "k3s manifests"; reality is no-k3s (`infra/no-k3s/`).

---

### Task 1: OpenCode Go inference migration — reference + overview docs

**Files:**
- Modify: `docs/reference/env-vars.md` (XNCH_ and NEXI_ sections)
- Modify: `docs/reference/api-xnch.md`
- Modify: `docs/reference/config-files.md`
- Modify: `docs/architecture/overview.md`
- Test: content assertions via `rg`

**Interfaces:**
- Consumes: Facts bank "OpenCode Go (xnch)" and "(nexi)".
- Produces: `#xnch_` rows `XNCH_OPENCODE_GO_API_URL/_API_KEY/_API_TIMEOUT_S`, `XNCH_LLM_STATUS_URL`, `XNCH_LLM_MODEL_ID`, `XNCH_LLM_PROBE_TIMEOUT_S`; `#nexi_` rows `NEXI_OPENCODE_GO_API_URL/_API_KEY/_API_TIMEOUT_S`, `NEXI_OPENCODE_GO_MODELS`. Tasks 3/4 cross-link these anchors.

- [ ] **Step 1: Confirm code facts before writing**

Run: `sed -n '90,110p' xnch/config.py && sed -n '14,45p' nexi/config.py`
Expected: `opencode_go_*` and `llm_*` fields exactly per Facts bank. If they differ, use file truth and note the delta in the commit body.

- [ ] **Step 2: Add env-var rows to `docs/reference/env-vars.md`**

In the `## XNCH_` table append:

```markdown
| `XNCH_OPENCODE_GO_API_URL` | `https://opencode.ai/zen/go/v1` | Base URL of the OpenCode Go API (hosted DeepSeek V4) used for gateway inference calls (`fcdedf1`) |
| `XNCH_OPENCODE_GO_API_KEY` | *(empty)* | Bearer key for the OpenCode Go API; required when hosted inference is in use |
| `XNCH_OPENCODE_GO_API_TIMEOUT_S` | `60.0` | Per-call timeout (seconds) for OpenCode Go requests |
| `XNCH_LLM_STATUS_URL` | `https://opencode.ai/zen/go/v1/models` | Endpoint probed by the `/system/llm-status` availability check |
| `XNCH_LLM_MODEL_ID` | `deepseek-v4-pro` | Model id used for chat — chat uses this configured id, never a routing label (`51a315c`) |
| `XNCH_LLM_PROBE_TIMEOUT_S` | `5.0` | Timeout for the llm-status probe |
```

In the `## NEXI_` table append:

```markdown
| `NEXI_OPENCODE_GO_API_URL` | `https://opencode.ai/zen/go/v1` | Base URL for option/model generation via OpenCode Go (`92bd45a`) |
| `NEXI_OPENCODE_GO_API_KEY` | *(empty)* | Bearer key for the OpenCode Go API |
| `NEXI_OPENCODE_GO_API_TIMEOUT_S` | `60.0` | Per-call timeout (seconds) |
| `NEXI_OPENCODE_GO_MODELS` | *(empty)* | Optional override of the opencode-go model catalog (JSON list of dicts); empty = catalog default |
```

- [ ] **Step 3: Add "Inference routing" section to `docs/reference/api-xnch.md`**

After the file intro, before the route tables:

```markdown
## Inference routing (2026-08-27)

Gateway inference calls are served by the **OpenCode Go API** (hosted DeepSeek V4) at
`XNCH_OPENCODE_GO_API_URL`, authenticated with `XNCH_OPENCODE_GO_API_KEY`. Local-first
routing sits on top of this (`3d54e21`): hosted is the default path; local vLLM/LiteLLM
endpoints remain configurable fallbacks. Chat always uses the configured
`XNCH_LLM_MODEL_ID` (`deepseek-v4-pro`) rather than an internal routing label.
Hosted-API liveness is exposed via `GET /system/llm-status`, probing `XNCH_LLM_STATUS_URL`.
```

- [ ] **Step 4: Config-file pointers in `docs/reference/config-files.md`**

Where the existing xnch/nexi config entries live, add:

```markdown
- xnch inference routing: `XNCH_OPENCODE_GO_*` + `XNCH_LLM_*` in `xnch/config.py` (see [env-vars](env-vars.md#xnch_))
- nexi inference routing: `NEXI_OPENCODE_GO_*` in `nexi/config.py` (see [env-vars](env-vars.md#nexi_))
```

- [ ] **Step 5: Routing paragraph in `docs/architecture/overview.md`**

In the component/model section:

```markdown
Model routing (2026-08-27): both services call the OpenCode Go hosted API
(`opencode.ai/zen/go/v1`, DeepSeek V4) for LLM work — xnch for gateway/chat
inference, nexi for option generation and per-task model selection
(`NEXI_OPENCODE_GO_MODELS` catalog override; stable-prefix prompt segmentation
enables API-side caching, `c375d47`). Local vLLM/LiteLLM tiers remain as
configured fallbacks; chat locks to `XNCH_LLM_MODEL_ID`.
```

- [ ] **Step 6: Verify content**

Run: `rg -c "OPENCODE_GO" docs/reference/env-vars.md docs/reference/api-xnch.md docs/reference/config-files.md docs/architecture/overview.md`
Expected: >= 2 matches in env-vars, >= 1 in each other file.

- [ ] **Step 7: Commit**

```bash
git add docs/reference/env-vars.md docs/reference/api-xnch.md docs/reference/config-files.md docs/architecture/overview.md
git commit -m "docs: OpenCode Go inference migration — env vars, routing, llm-status model id"
```

---

### Task 2: xnch_mcp chat-loop behavior notes

**Files:**
- Modify: `docs/reference/mcp-tools.md`
- Test: content assertion via `rg`

**Interfaces:**
- Consumes: Facts bank "xnch_mcp (08-28)".
- Produces: behavioral notes under the tools table; no anchors needed by later tasks.

- [ ] **Step 1: Read the tool table context**

Run: `sed -n '60,90p' docs/reference/mcp-tools.md`
Expected: the `xnch_fs_read` row (~line 76) with tier/roles columns.

- [ ] **Step 2: Add behavior notes directly below the tools table**

```markdown
### File-system tool behavior (2026-08-28)

- **Concrete paths shortcut:** a request that already names a concrete file path is routed
  straight to `xnch_fs_read` instead of going through the planning loop (`75c7380`).
- **Recursive glob hygiene:** `xnch_fs_glob` prunes noise directories (caches, venvs,
  build artifacts) from recursive globs (`5a33c09`).
- **Chat loop tool results:** tool results are surfaced in the chat loop output, and Office
  documents (`.docx`/`.xlsx`/`.pptx`) are text-extracted inline so their content is readable
  in replies (`d2ca850`).
- **`tool_choice` constraint:** requests to the OpenCode Go backend never include unsupported
  `tool_choice` parameters (`3b18021`).
```

- [ ] **Step 3: Verify**

Run: `rg -c "concrete file path|Office" docs/reference/mcp-tools.md`
Expected: >= 1 match each.

- [ ] **Step 4: Commit**

```bash
git add docs/reference/mcp-tools.md
git commit -m "docs: xnch_mcp fs routing, office extraction, tool_choice notes"
```

---

### Task 3: xtrain Phase 1 training cycle

**Files:**
- Modify: `docs/architecture/training.md` (new `## Phase 1 — training cycle` section)
- Modify: `docs/reference/env-vars.md` (XTRAIN_ section)
- Modify: `docs/index.md` (only if Step 4 shows a stale description)
- Test: content assertions + link check

**Interfaces:**
- Consumes: Facts bank "xtrain Phase 1" and "xtrain config defaults"; existing `docs/runbooks/node-b-hardware-gate.md` (linked, not edited).
- Produces: anchor `#phase-1-training-cycle` in `training.md`.

- [ ] **Step 1: Confirm code facts**

Run: `sed -n '30,50p' xnch-train/xnch_train/config.py && ls xnch-train/xnch_train/train/`
Expected: config fields per Facts bank; `cycle.py merge.py promote.py qlora.py registry.py` present.

- [ ] **Step 2: Append Phase 1 section to `docs/architecture/training.md`**

After the existing `## Phase 1 environment` section:

````markdown
## Phase 1 — training cycle (2026-08-27)

The full Train → Merge → Register → Propose loop is orchestrated end-to-end:

| Stage | Module | Notes |
|-------|--------|-------|
| Train | `train/qlora.py` | QLoRA SFT of Ornith-1.0-35B; G1 resolution included (`1ce8a38`) |
| Merge | `train/merge.py` | Adapter merge + GPTQ requantization to a serving artifact (`a218c61`) |
| Register | `train/registry.py` | Immutable checkpoint registry — registered checkpoints are never mutated (`a218c61`) |
| Propose | `client.py` | Goal claim via `POST /goals/claim` (raise_for_status enforced, `07b7c2a`) + HITL promotion proposal through the standard approval path (`362e9e5`) |

Cycle execution is GPU-exclusive: cycle and promote units are scheduled with GPU
conflict wiring (3-way conflicts, `3ea7514`), guarded by a cycle lockfile
(`_acquire_lock`, `train/cycle.py`), so two cycles cannot run concurrently.

### CLI

```bash
uv run xtrain cycle      # full Train→Merge→Register→Propose orchestration (b132795)
uv run xtrain promote    # symlink flip + smoke check + rollback (58a982e)
```

`promote` flips the serving symlink to the newly registered checkpoint, runs a smoke
check, and can roll back. Cycle telemetry (traces, GPU contention, disk-quota alarm)
is emitted per cycle (`11418c3`).

Hardware gating for Node B is covered in the
[Node B hardware-gate runbook](../runbooks/node-b-hardware-gate.md).
````

- [ ] **Step 3: Add XTRAIN_ rows to `docs/reference/env-vars.md`**

In the `## XTRAIN_` section append (defaults from `xnch-train/xnch_train/config.py`):

```markdown
| `XTRAIN_POSTGRES_URL` | `postgresql://localhost:5432/xnch` | Outcome/correction extraction source |
| `XTRAIN_DATASET_DIR` | `./datasets` | Dataset output directory |
| `XTRAIN_SERVING_REGRESSION_BOUND_PCT` | `10.0` | Promotion gate: max tolerated serving regression (%) |
| `XTRAIN_EXTRACT_PAGE_SIZE` | `100` | Page size for extractor queries |
```

Keep existing `XTRAIN_PSEUDONYMIZE_SECRET`, `XTRAIN_GATE_EPSILON`, `XTRAIN_LANGFUSE_*`, `XTRAIN_VENV` rows.

- [ ] **Step 4: Check `docs/index.md`**

Run: `rg -n "training" docs/index.md`
Expected: a line for `architecture/training.md`. If its description says only "Phase 0", update it to mention the Phase 1 cycle; otherwise no change.

- [ ] **Step 5: Verify content + link**

Run:
```bash
rg -c "Phase 1 — training cycle|promote" docs/architecture/training.md
test -e docs/runbooks/node-b-hardware-gate.md && echo LINK-OK
```
Expected: matches found; `LINK-OK` printed.

- [ ] **Step 6: Commit**

```bash
git add docs/architecture/training.md docs/reference/env-vars.md docs/index.md
git commit -m "docs: xtrain Phase 1 cycle — orchestrator, promote, registry, gate env vars"
```

---

### Task 4: Nexi dynamic persona + refresh timer

**Files:**
- Create: `docs/guides/nexi-persona.md`
- Create: `docs/runbooks/persona-refresh.md`
- Modify: `docs/reference/env-vars.md` (NEXI_ section)
- Modify: `docs/architecture/overview.md` (one-line pointer)
- Test: content assertions + link check

**Interfaces:**
- Consumes: Facts bank "nexi persona"; Task 1's `#nexi_` env anchor.
- Produces: `docs/guides/nexi-persona.md` referenced from `docs/architecture/overview.md`; `docs/runbooks/persona-refresh.md` following the repo runbook pattern (Deploy/Validate/Rollback + footgun).

- [ ] **Step 1: Write `docs/guides/nexi-persona.md`**

````markdown
# Nexi persona — dynamic & realtime (2026-08-27)

Nexi's persona is 100% dynamic: `persona.yaml`, `capabilities.yaml`, and
`identity_facts.yaml` under `nexi/character/` are inputs; the rendered overlay is
written to `NEXI_PERSONA_GENERATED_PATH` (`~/.xnch/nexi-persona.generated.yaml` by
default) and reloaded at runtime (`f84a8e9`). Model selection is per-task via the
OpenCode Go catalog (`NEXI_OPENCODE_GO_MODELS` override — see
[env-vars](../reference/env-vars.md#nexi_)). Prompt construction uses stable-prefix
segmentation so the API can cache the unchanged prefix (`c375d47`).

## Manual refresh

Run: `~/venvs/xnch/bin/python -m nexi.character.refresh --verbose`
Expected: prints overlay change status; exit 0. (The xnch venv is used so
`prometheus_client` resolves — `25e937c`.)

Spec: [dynamic nexi persona design](../superpowers/specs/2026-08-27-dynamic-nexi-persona-design.md)
````

- [ ] **Step 2: Write `docs/runbooks/persona-refresh.md`**

````markdown
# Runbook — persona refresh timer (node-a)

Systemd timer deploys the nexi persona/capability refresh (`f6d7d9d`):
`infra/no-k3s/node-a/systemd/nexi-persona-refresh.timer` (+ service unit alongside).

## Deploy

Run: `sudo systemctl daemon-reload && sudo systemctl enable --now nexi-persona-refresh.timer`
Expected: `systemctl status nexi-persona-refresh.timer` shows active (waiting).

## Validate

Run: `systemctl list-timers nexi-persona-refresh.timer && journalctl -u nexi-persona-refresh.service -n 20`
Expected: timer listed with next trigger; service log shows the refresh completing with exit 0.

## Rollback

Run: `sudo systemctl disable --now nexi-persona-refresh.timer`
Expected: timer removed from `list-timers`; manual refresh remains available
(see [persona guide](../guides/nexi-persona.md)).

Footgun: the service must run under the xnch venv — if you see
`ModuleNotFoundError: prometheus_client`, the unit is using the wrong interpreter (`25e937c`).
````

- [ ] **Step 3: Env rows + overview pointer**

Append to the `## NEXI_` table in `docs/reference/env-vars.md`:

```markdown
| `NEXI_PERSONA_AUTO_REFRESH` | `true` | Reload persona overlay automatically on change (`persona_auto_refresh`) |
| `NEXI_PERSONA_GENERATED_PATH` | `~/.xnch/nexi-persona.generated.yaml` | Rendered persona overlay path |
```

In `docs/architecture/overview.md`, next to the nexi component description, add:

```markdown
Persona is dynamic & realtime — see [nexi persona guide](../guides/nexi-persona.md).
```

- [ ] **Step 4: Verify content + links**

Run:
```bash
rg -c "persona" docs/guides/nexi-persona.md docs/runbooks/persona-refresh.md
test -e docs/superpowers/specs/2026-08-27-dynamic-nexi-persona-design.md && test -e infra/no-k3s/node-a/systemd/nexi-persona-refresh.timer && echo LINK-OK
```
Expected: matches found; `LINK-OK` printed.

- [ ] **Step 5: Commit**

```bash
git add docs/guides/nexi-persona.md docs/runbooks/persona-refresh.md docs/reference/env-vars.md docs/architecture/overview.md
git commit -m "docs: nexi dynamic persona guide + node-a persona refresh timer runbook"
```

---

### Task 5: Workflow canvas builder + server-side HITL gate

**Files:**
- Modify: `docs/guides/build-workflow.md` (canvas builder UI)
- Modify: `docs/architecture/workflows-hitl.md` (server-side enforcement)
- Modify: `docs/guides/operate-hitl.md` (operator note)
- Test: content assertions

**Interfaces:**
- Consumes: Facts bank "Workflows (08-24)".
- Produces: no cross-task anchors.

- [ ] **Step 1: Read current build-workflow guide**

Run: `head -60 docs/guides/build-workflow.md`
Expected: existing canvas-less builder instructions (form/JSON based, 08-23 vintage).

- [ ] **Step 2: Add canvas section to `docs/guides/build-workflow.md`**

```markdown
## Canvas builder (2026-08-24)

The Muse UI (`web/`) ships a drag-and-drop workflow canvas: nodes are placed by
dragging, connections are drawn between node ports, and the graph is saved through
the same gateway-gated workflow API the JSON path uses (`a059090`). Both authoring
paths produce identical workflow definitions — mixed editing is safe.
```

- [ ] **Step 3: Add server-side gating note to `docs/architecture/workflows-hitl.md`**

```markdown
Server-side enforcement (2026-08-24): HITL gating is enforced by xnch itself, not
only by the UI — workflows that require approval cannot advance past a gated step
server-side (`fb82192`). Clients must not (and cannot) bypass the gate by calling
advance routes directly.
```

- [ ] **Step 4: Operator note in `docs/guides/operate-hitl.md`**

```markdown
Workflows are HITL-gated server-side as of 2026-08-24 (`fb82192`): approval
requirements are enforced by xnch regardless of which client drives the run.
```

- [ ] **Step 5: Verify**

Run: `rg -c "canvas|drag" docs/guides/build-workflow.md && rg -c "server-side|server side" docs/architecture/workflows-hitl.md docs/guides/operate-hitl.md`
Expected: >= 1 match in each file.

- [ ] **Step 6: Commit**

```bash
git add docs/guides/build-workflow.md docs/architecture/workflows-hitl.md docs/guides/operate-hitl.md
git commit -m "docs: workflow canvas builder + server-side HITL gating"
```

---

### Task 6: Top-level repo docs accuracy (AGENTS.md + README.md)

**Files:**
- Modify: `AGENTS.md` (Multi-Repo Structure section)
- Modify: `README.md` (directory tree, add `agent-runner/` line)
- Test: content assertions

**Interfaces:**
- Consumes: Facts bank "Top-level structure".
- Produces: no cross-task anchors.

- [ ] **Step 1: Read current sections**

Run: `head -30 AGENTS.md && sed -n '40,55p' README.md`
Expected: AGENTS.md lists only `xnch/ nexi/ infra/ docs/ scripts/ misc/` and says "k3s manifests"; README tree lacks `agent-runner/`.

- [ ] **Step 2: Update `AGENTS.md` Multi-Repo Structure**

Replace the stale bullet list with:

```markdown
- `xnch/` — git submodule → github.com/x-nch/xnch (control plane API: REST routes, auth, memory, policy, learning; no-k3s deploy under `infra/no-k3s/`)
- `nexi/` — git submodule → github.com/x-nch/nexi (execution engine: FastAPI, decision/policy pipeline, dynamic persona)
- `web/` — Next.js muse UI (chat, HITL approvals, drag-and-drop workflow canvas)
- `xnch_mcp/` — MCP server + federated bridge (`xnch_*` native tools, `crg_`/`am_`/`doc_` passthroughs)
- `xnch-train/` — training data pipeline + eval harness (Phase 0 gates; Phase 1 QLoRA cycle)
- `agent-runner/` — stdlib dispatch runner (Mac-side opencode runner pulling from xnch) + launchd template
- `scraper/` — tiered web scraper service (Playwright; pgvector store)
- `infra/` — no-k3s deployment: systemd units, docker-compose, observability stack, wake/start scripts
- `docs/` — architecture docs, runbooks, guides, reference
- `scripts/` — helper scripts, migration agent
- `misc/` — historical records, conversations, reports
```

- [ ] **Step 3: Add `agent-runner/` to the `README.md` tree**

Insert alphabetically in the tree listing (matching its comment style):

```markdown
agent-runner/    Mac-side opencode dispatch runner + launchd template
```

- [ ] **Step 4: Verify**

Run: `rg -c "agent-runner|no-k3s|workflow canvas" AGENTS.md && rg -c "agent-runner" README.md`
Expected: >= 2 in AGENTS.md, >= 1 in README.md.

- [ ] **Step 5: Commit**

```bash
git add AGENTS.md README.md
git commit -m "docs: sync AGENTS.md structure section + README tree with current repo layout"
```

---

## Self-Review (performed 2026-09-02)

1. **Spec/audit coverage:** 08-27/28 commit groups → Task mapping: OpenCode Go migration (fcdedf1/92bd45a/3d54e21/a40d63e/51a315c/c375d47) → T1; xnch_mcp fixes (75c7380/5a33c09/d2ca850/3b18021) → T2; xtrain Phase 1 (3416a4f…11418c3) → T3; nexi persona + timer (f84a8e9/eefe005/f6d7d9d/25e937c) → T4; canvas + server-side gate (a059090/fb82192) → T5; repo structure (README/AGENTS staleness) → T6. Documented-already commits (dispatch security 7e088c0, observability runbook 9a2451a/0607dbc, elizaos-fit 73e2bbd, node-b runbook c905c30, agentic-10-10 c4d6e22/7c7a778, muse runs doc 3c3b5fe) intentionally out of scope.
2. **Placeholder scan:** none — every step carries verbatim content or an exact verification command; conditional steps (T3 Step 4, T6 Step 1) specify the exact condition and outcome.
3. **Type/name consistency:** env var names match submodule `config.py` field names upper-cased per repo prefix rules (`persona_auto_refresh` → `NEXI_PERSONA_AUTO_REFRESH`, `serving_regression_bound_pct` → `XTRAIN_SERVING_REGRESSION_BOUND_PCT`); CLI verbs `cycle`/`promote` verified present in `xnch-train/xnch_train/cli.py`; all referenced file paths verified to exist.
