# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg

# Local runtime data
memory/
.data/
.env
.env.*

# Code review graph
.code-review-graph/

# Agent logs
scripts/logs/

# local runtime artifacts
.chroma_db/
storage/
SESSIONS

# agent-run deliverables (never commit run output to the repo)
/response.md
/recommended-roles.md
/xnch-agents/

# web lib layer — negate global gitignore's bare `lib/` pattern which would
# otherwise silently drop web/src/lib/** from untracked adds
!web/src/lib/
!web/src/lib/**

# git worktrees
.worktrees/
