{
  "name": "Nightly Knowledge Graph Refresh",
  "description": "Rebuild the code-review-graph, record stats to memory, and drop a daily wiki digest. Scheduled nightly.",
  "owner_actor_id": "operator",
  "trigger": { "kind": "schedule", "cron": "0 3 * * *" },
  "steps": [
    {
      "id": "build_graph",
      "kind": "exec_tool",
      "summary": "Rebuild code-review-graph for the repo root",
      "target": "repo",
      "args": { "command": "python -m code_review_graph build_or_update --repo-root ." },
      "preview": "python -m code_review_graph build_or_update --repo-root .",
      "requires_approval": true,
      "description": "Elevated: regenerates the knowledge graph. Requires admin approval in /approvals."
    },
    {
      "id": "record_stats",
      "kind": "update_memory",
      "summary": "Record latest graph stats to memory",
      "target": "graph_stats",
      "args": { "note": "nightly graph rebuild completed" },
      "requires_approval": false,
      "description": "Low-risk: executor auto-runs after the build step is approved."
    },
    {
      "id": "write_digest",
      "kind": "write_file",
      "summary": "Write the daily wiki digest",
      "target": "reports/graph-digest.md",
      "args": { "content": "# Graph Digest\n\nGenerated by nightly workflow.\n" },
      "requires_approval": false
    }
  ]
}
