# Minimal deps for Mac voice CLI (mic/speaker local, API on gate7).
# Install: uv pip install -r requirements-mac-voice-client.txt
# Run from repo root with: export PYTHONPATH="$PWD:$PWD/xnch"
httpx>=0.28
typer>=0.12
pyjwt>=2.8
click>=8.0
PyYAML>=6.0
sounddevice>=0.5
numpy>=1.26
